@host = http://localhost:3000

### Health Check
GET {{host}}/health

### Liveness Probe (Kubernetes)
GET {{host}}/health/live

### Readiness Probe (Kubernetes)
GET {{host}}/health/ready

### Not Found (verify error response format)
GET {{host}}/non-existent-route
