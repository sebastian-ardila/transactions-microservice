apiVersion: v1
kind: Service
metadata:
  name: transactions-api
  labels:
    app: transactions-api
spec:
  type: ClusterIP
  selector:
    app: transactions-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
